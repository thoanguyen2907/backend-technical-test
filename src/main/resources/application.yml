server:
  port: 8080
spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://code4fun.xyz:5432/postgres?currentSchema=shopify
    username: dev
    password: P@ssword*123
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: false



